import numpy as np


def chay_momentum(grad, w0, lr, beta, T):
    w = np.array(w0, dtype=float)
    v = np.zeros_like(w)
    duong = [w.copy()]
    # Viết code của bạn ở đây: lặp T lần, cập nhật v rồi w, thêm w.copy() vào duong
    raise NotImplementedError
