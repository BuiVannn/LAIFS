import numpy as np


def buoc_rnn(h, x, Wh, Wx, b):
    # Viết code của bạn ở đây: h_moi = tanh(Wh @ h + Wx @ x + b)
    raise NotImplementedError


def ma_hoa(X, Wh, Wx, b):
    # Viết code của bạn ở đây: chạy buoc_rnn qua từng hàng của X, h0 = 0
    # Trả về (H, c)
    raise NotImplementedError
