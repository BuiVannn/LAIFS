import numpy as np


def lop_tuyen_tinh(X, W, b):
    """X (n, d_vao), W (d_ra, d_vao), b (d_ra,) -> (n, d_ra)."""
    # Viết code của bạn ở đây
    raise NotImplementedError


def mlp_xuoi(X, tham_so):
    """tham_so: danh sách [(W1, b1), (W2, b2), ...]. ReLU giữa các lớp, lớp cuối để nguyên."""
    # Viết code của bạn ở đây
    raise NotImplementedError
