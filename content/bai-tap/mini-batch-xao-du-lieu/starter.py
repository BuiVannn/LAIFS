import numpy as np


def mot_epoch(x, y, w, lr, B, rng):
    n = len(x)
    thu_tu = rng.permutation(n)
    # Viết code của bạn ở đây: lặp qua từng nhóm cỡ B, tính gradient, cập nhật w
    raise NotImplementedError
