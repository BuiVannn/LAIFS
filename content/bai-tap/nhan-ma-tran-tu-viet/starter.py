import numpy as np


def tich_vo_huong(u, v):
    """u, v shape (n,) -> trả về một số."""
    # Viết code của bạn ở đây
    raise NotImplementedError


def nhan(A, B):
    """A (m, n) @ B (n, p) -> (m, p). Chiều trong không khớp thì raise ValueError."""
    # Viết code của bạn ở đây
    raise NotImplementedError


def cot_ket_qua(A, B, j):
    """Cột j của AB, tính theo tổ hợp tuyến tính các cột của A. -> shape (m,)"""
    # Viết code của bạn ở đây
    raise NotImplementedError
