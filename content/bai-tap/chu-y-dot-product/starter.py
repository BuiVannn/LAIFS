import numpy as np


def softmax(x, axis=-1):
    # Viết code của bạn ở đây. Nhớ trừ đi max theo axis trước khi lấy exp.
    # Gợi ý: np.max(x, axis=axis, keepdims=True)
    raise NotImplementedError


def chu_y(H, s):
    # Viết code của bạn ở đây: điểm -> softmax -> vector ngữ cảnh
    # Trả về (alpha, c)
    raise NotImplementedError
