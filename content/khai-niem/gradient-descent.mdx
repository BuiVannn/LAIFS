---
ten_vi: Gradient Descent
ten_en: Gradient Descent
tang: 2
thu_tu: 12
tien_quyet: [dao-ham-rieng-gradient, ham-mat-mat]
trang_thai: nhap
dinh_nghia: >-
  Thuật toán tìm cực tiểu của một hàm (thường là hàm mất mát) bằng cách lặp đi lặp lại:
  tính độ dốc tại điểm hiện tại, rồi bước một bước nhỏ theo hướng ngược với độ dốc.
truc_giac: >-
  Bạn đứng trên sườn núi trong sương mù dày, không nhìn thấy đáy thung lũng. Cách duy nhất
  là dò dưới chân xem hướng nào dốc xuống, bước một bước theo hướng đó, rồi dò lại.
  Độ dốc là đạo hàm, độ dài bước là learning rate.
hieu_nham:
  - "Gradient chỉ hướng đi LÊN dốc nhanh nhất, vì vậy phải trừ đi gradient (đi ngược hướng) mới xuống được."
  - "Learning rate càng lớn chưa chắc càng nhanh: quá lớn thì nhảy qua đáy, dao động hoặc phân kỳ (loss tăng vọt)."
  - "Gradient descent không đảm bảo tìm được cực tiểu toàn cục: với hàm không lồi nó có thể dừng ở cực tiểu cục bộ, tuỳ điểm xuất phát."
  - "Bước đi tự nhỏ lại khi gần đáy dù learning rate cố định, vì độ dốc (gradient) nhỏ dần."
y_chinh:
  - Mục tiêu là tìm tham số làm hàm mất mát nhỏ nhất
  - "Đạo hàm/gradient cho biết hướng dốc lên, nên đi ngược lại: w ← w − η·L′(w)"
  - Learning rate quyết định độ dài bước; quá nhỏ thì chậm, quá lớn thì dao động hoặc phân kỳ
  - Bước tự ngắn lại khi gần đáy vì độ dốc nhỏ dần
  - Có thể kẹt ở cực tiểu cục bộ, tuỳ điểm xuất phát
tai_lieu:
  - url: https://www.youtube.com/watch?v=sDv4f4s2SB8
    tieu_de: Gradient Descent, Step-by-Step
    tac_gia: StatQuest with Josh Starmer
    loai: video
    muc_do: nhap_mon
    ngon_ngu: en
    ghi_chu: Tính tay từng bước trên ví dụ hồi quy tuyến tính, rất chậm và rõ. Nên xem đầu tiên.
  - url: https://www.youtube.com/watch?v=IHZwWFHWa-w
    tieu_de: Gradient descent, how neural networks learn
    tac_gia: 3Blue1Brown
    loai: video
    muc_do: nhap_mon
    ngon_ngu: en
    ghi_chu: Hình ảnh hoá gradient descent trong mạng neuron với hàng nghìn tham số. Xem sau khi đã hiểu bản một biến.
  - url: https://www.youtube.com/watch?v=VMj-3S1tku0
    tieu_de: "The spelled-out intro to neural networks and backpropagation: building micrograd"
    tac_gia: Andrej Karpathy
    loai: video
    muc_do: sau
    ngon_ngu: en
    ghi_chu: Tự code gradient descent và backprop từ số 0 bằng Python. Dài (~2.5 giờ), dành cho khi muốn đào sâu.
---

import GradientDescent1D from '../../src/viz/GradientDescent1D.tsx';
import CauHoi from '../../src/components/CauHoi.astro';

## Bài toán: tìm đáy

Huấn luyện một mô hình ML thực chất là đi tìm bộ tham số (ví dụ $w$) làm cho **hàm mất mát** (loss function) $L(w)$ nhỏ nhất. Loss nhỏ nghĩa là mô hình dự đoán sai ít.

Với hàm đơn giản như $L(w) = w^2$, ta nhìn là biết đáy ở $w = 0$. Nhưng mô hình thật có hàng triệu tham số, không thể vẽ ra hay giải trực tiếp. Ta cần một cách **mò dần** xuống đáy. Đó là gradient descent.

## Ý tưởng: đi ngược độ dốc

Đạo hàm $L'(w)$ cho biết độ dốc tại điểm đang đứng:

- $L'(w) > 0$: đi sang phải thì loss **tăng**, vậy nên đi sang **trái** (giảm $w$)
- $L'(w) < 0$: đi sang phải thì loss **giảm**, vậy nên **tăng** $w$

Cả hai trường hợp gói gọn trong một công thức cập nhật:

$$
w \leftarrow w - \eta \cdot L'(w)
$$

trong đó $\eta$ (đọc là "eta") là **learning rate**: bước đi dài hay ngắn.

<CauHoi cau="gradient-descent/q2" />

## Tính tay 3 bước

Lấy $L(w) = w^2$, nên $L'(w) = 2w$. Chọn $\eta = 0.1$, xuất phát từ $w = 2.5$:

| Bước | $w$ | $L'(w) = 2w$ | $w$ mới $= w - 0.1 \cdot L'(w)$ |
|---|---|---|---|
| 1 | 2.5 | 5.0 | 2.0 |
| 2 | 2.0 | 4.0 | 1.6 |
| 3 | 1.6 | 3.2 | 1.28 |

Để ý: bước 1 đi được 0.5, bước 3 chỉ còn 0.32. **Càng gần đáy, dốc càng thoải, bước tự ngắn lại**, dù $\eta$ không đổi.

<CauHoi cau="gradient-descent/q3" />

## Tự thử: đoán trước, rồi chạy

Mỗi thí nghiệm dưới đây đã cố định tham số. **Chọn dự đoán trước** thì mới chạy được: đoán sai rồi tự thấy kết quả sẽ nhớ lâu hơn nhiều so với chỉ xem.

<GradientDescent1D
  client:visible
  ham="parabol"
  lr={0.01}
  w0={2.5}
  duDoan={{
    de: "Learning rate rất nhỏ (η = 0.01). Sau 20 bước, w sẽ ở đâu?",
    lua_chon: ["Đã tới rất gần đáy (w ≈ 0)", "Mới xuống được một đoạn, còn xa đáy", "Vượt qua đáy sang phía âm"],
    dap_an: 1,
    giai_thich: "Mỗi bước chỉ nhân w với (1 − 0.02) = 0.98, nên sau 20 bước w ≈ 2.5 × 0.98²⁰ ≈ 1.67. Hướng đúng nhưng quá chậm."
  }}
/>

<GradientDescent1D
  client:visible
  ham="parabol"
  lr={1.0}
  w0={2.5}
  duDoan={{
    de: "η = 1.0, xuất phát w = 2.5. Điều gì xảy ra?",
    lua_chon: ["Hội tụ về 0", "Nhảy qua lại mãi giữa 2.5 và −2.5", "Phân kỳ, văng ra xa"],
    dap_an: 1,
    giai_thich: "w ← w − 1.0 × 2w = −w: mỗi bước chỉ đổi dấu, không bao giờ gần đáy hơn. Nếu tăng lên η = 1.05 thì w ← −1.1w và phân kỳ."
  }}
/>

<GradientDescent1D
  client:visible
  ham="nhieu"
  lr={0.1}
  w0={2.8}
  duDoan={{
    de: "Hàm có hai đáy: đáy trái (w ≈ −2.35) sâu hơn đáy phải (w ≈ 2.1). Xuất phát từ w = 2.8, gradient descent dừng ở đâu?",
    lua_chon: ["Đáy trái, vì sâu hơn", "Đáy phải", "Đỉnh ở giữa (w ≈ 0.25)"],
    dap_an: 1,
    giai_thich: "Gradient descent chỉ nhìn độ dốc tại chỗ đang đứng, nên rơi vào đáy gần nhất: cực tiểu cục bộ bên phải. Thử ở bảng tự do bên dưới với w₀ = −1 để tới đáy trái."
  }}
/>

Bảng tự do, chỉnh gì cũng được:

<GradientDescent1D client:visible />

## Khi có nhiều tham số

Mô hình thật có nhiều tham số $w_1, w_2, \dots$. Khi đó thay đạo hàm bằng **gradient**, tức là vector gồm các đạo hàm riêng:

$$
\nabla L = \left( \frac{\partial L}{\partial w_1}, \frac{\partial L}{\partial w_2}, \dots \right)
\qquad
\mathbf{w} \leftarrow \mathbf{w} - \eta \, \nabla L(\mathbf{w})
$$

Ý tưởng giữ nguyên: mỗi tham số tự bước ngược hướng dốc của riêng nó, tất cả **cùng lúc**.

<CauHoi cau="gradient-descent/q9" />

## Khi nào dừng?

Thường dừng khi một trong các điều kiện xảy ra:

- Chạy đủ số bước (epoch) định trước
- Loss gần như không giảm nữa
- Gradient gần bằng 0

## Tóm tắt

- Gradient descent: lặp lại $w \leftarrow w - \eta \nabla L$ để đi xuống đáy hàm mất mát.
- Learning rate quá nhỏ thì chậm, quá lớn thì dao động hoặc phân kỳ.
- Với hàm không lồi, kết quả phụ thuộc điểm xuất phát.
- Tiếp theo (sắp có): Learning rate, SGD & mini-batch.
