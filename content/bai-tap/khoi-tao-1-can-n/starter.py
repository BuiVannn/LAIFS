import numpy as np


def khoi_tao(kich_thuoc, rng, sigma_w=None):
    """[d0, ..., dL] -> danh sách L ma trận, ma trận i có shape (d[i+1], d[i])."""
    # Viết code của bạn ở đây
    raise NotImplementedError


def std_qua_lop(X, Ws):
    """Trả về danh sách len(Ws) + 1 số: std của X, rồi std sau từng lớp H = H @ W.T."""
    # Viết code của bạn ở đây
    raise NotImplementedError
