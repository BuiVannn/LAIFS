from collections import Counter


def hoc_merge(dem_tu, so_merge):
    # tu = {"low": ["l", "o", "w"], ...} rồi lặp so_merge lần
    raise NotImplementedError


def ap_dung(merges, tu):
    # hang = {cap: thu_hang}; mỗi vòng gộp cặp có thứ hạng nhỏ nhất
    raise NotImplementedError
