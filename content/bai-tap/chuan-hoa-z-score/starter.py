import numpy as np


def hoc_thong_ke(X_train):
    """X_train (n, d) -> (mu, sigma), mỗi cái shape (d,)."""
    # Viết code của bạn ở đây
    raise NotImplementedError


def ap_dung(X, mu, sigma):
    """X (n, d) -> (n, d) đã chuẩn hoá bằng mu, sigma cho trước."""
    # Viết code của bạn ở đây
    raise NotImplementedError
